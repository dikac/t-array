{
  "name": "@dikac/t-array",
  "version": "0.14.0",
  "description": "Typescript array",
  "dependencies": {
    "@dikac/t-boolean": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^12.12.24",
    "typescript": "^3.7.4",
    "@types/jasmine": "^3.5.0",
    "npm-run-all": "^4.1.5",
    "json-properties-remover": "^0.1.2",
    "jasmine": "^3.4.0",
    "move-cli": "^1.2.1",
    "cpy-cli": "^3.0.0",
    "rimraf": "^3.0.0"
  },
  "scripts": {
    "test": "jasmine --config=./jasmine.json",
    "test-compile": "npm-run-all --parallel test-compile-script ",
    "test-compile-watch": "npm-run-all --parallel test-compile-watch-script",
    "test-compile-script": "tsc -p tsconfig-spec.json",
    "test-compile-watch-script": "tsc -w -p tsconfig-spec.json",
    "compile": "tsc",
    "compile-watch": "tsc -w",
    "release": "remove-json-properties dist/package.json private",
    "postrelease": "npm publish dist",
    "prerelease": "cpy     package.json package-lock.json LICENSE Readme.md .npmignore dist",
    "postpublish": "rimraf package.json package-lock.json LICENSE Readme.md .npmignore"
  },
  "author": "",
  "private": true,
  "license": "MIT"
}
