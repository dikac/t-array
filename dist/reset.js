/**
 * reset array index
 * @param argument
 */
export default function Reset(argument) {
    const buffer = [];
    argument.forEach(function (v, i) {
        buffer.push(v);
    });
    return buffer;
}
//# sourceMappingURL=reset.js.map